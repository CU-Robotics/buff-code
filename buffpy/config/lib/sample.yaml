DEBUG:
  True
  
TOPICS:
  RAW_IMG:
    'image_raw'
  DETECTED:
    'detected_boundary'
    
NODES:
  # ROSBAGGER:    # This will record all system runs and allow us to play them back
  #   FILE:
  #     'record'
  #   PACKAGE:
  #     'rosbag'
  #   ARGS:       # -a record all -x ignore -o output prefix
  #     - '-a'
  #     - '-x /rosout'
  #     - '-o /home/cu-robotics/buff-code/data/'
  CAMERA:     # spawns our camera streamer
    FILE:
      'mds_camera.py'
    TOPICS:
      - 'RAW_IMG'
    CONFIG:
      'default_camera.yaml'
    PACKAGE:
      'buffpy'
  DETECTOR:   # spawns Mitchell's home-made detector
    FILE:
      'mds_detector.py'
    TOPICS:
      - 'RAW_IMG'
      - 'DETECTED'
    CONFIG:
      'bouncing_ball.yaml'
    PACKAGE:
      'buffpy'